---
description: OpenMemory 作为长期上下文的记忆工具 — 每次运行前搜索、完成后更新
alwaysApply: true
---

# OpenMemory 记忆工作流（强制）

**OpenMemory 是本项目的长期上下文记忆工具。** 必须按以下流程使用：

## 1. 运行大模型之前 — 搜索记忆

在开始执行用户任务、调用 LLM 或编写代码之前：

- 使用 `search_memory` 根据任务关键词搜索相关记忆
- 将检索到的记忆纳入上下文，用于理解用户偏好、项目约定、历史决策
- 搜索 query 应覆盖：任务主题、技术栈、项目名、用户习惯等

## 2. 完成任务之后 — 自动更新记忆

在完成用户请求、解决 bug、实现功能或做出重要决策之后：

- 使用 `add_memories` 记录本次任务中有价值的信息，例如：
  - 用户偏好与约定
  - 技术选型与架构决策
  - 踩坑与解决方案
  - 项目结构、命名规范
  - 待办或后续计划

## 3. 原则

- **先搜后做**：避免重复劳动，利用已有记忆
- **做完必记**：有价值的信息及时写入，供后续会话使用
- **精简准确**：记忆内容应简洁、可检索，避免冗余
