---
description: OpenMemory Python 后端开发规范
globs: openmemory/api/**/*.py
alwaysApply: false
---

# OpenMemory Python 后端规范

## 架构概览

- **框架**: FastAPI + SQLAlchemy (SQLite)
- **向量存储**: Qdrant（`mem0_store` 容器，端口 6333）
- **LLM/Embedding**: Ollama (本地) 或 OpenAI (云端)，通过 CATEGORIZATION_PROVIDER 配置
- **MCP Server**: SSE 传输，端口 8765

## 关键路径

| 模块 | 职责 |
|------|------|
| `routers/memories.py` | REST API 端点 |
| `mcp_server.py` | MCP 工具实现 |
| `models.py` | SQLAlchemy 模型 + 后台分类 |
| `utils/categorization.py` | LLM 分类调用 (Ollama/OpenAI) |
| `utils/memory.py` | mem0 Memory 客户端封装 |

## 开发约定

- Qdrant payload **统一使用 snake_case**（`user_id`, `created_at`），绝不使用 camelCase
- 新增 API 端点必须在 MCP server 中也提供对应工具（保持双通道一致）
- 后台任务（如分类）使用 FastAPI `BackgroundTasks`；MCP 中用 `threading.Thread(daemon=True)`
- 所有记忆创建/更新后必须触发 `categorize_memory_background`
- Docker 容器访问宿主机服务用 `host.docker.internal`（macOS）或 `get_docker_host_url()`

## 错误处理

- 外部服务调用（Ollama、Qdrant）必须 try/except，降级而非崩溃
- MCP 工具返回字符串错误消息（不抛异常），格式 `"Error: <描述>"`
- REST API 使用 HTTPException 标准状态码
