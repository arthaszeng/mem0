---
description: Docker 部署、构建与版本管理规范
globs: docker-compose*.yml, **/Dockerfile, VERSION, .env*, deploy/**
alwaysApply: true
---

# Docker 部署与版本管理规范

## ⚠️ 铁律：容器启动位置

**严禁在 `openmemory/` 子目录下启动 Docker 容器！**

- 所有 Docker 操作必须在**项目根目录 `./mem0`** 下执行
- 本地部署：`./deploy/deploy-local.sh`（使用 `docker-compose.local.yml`）
- 云端部署：`./deploy/deploy-cloud.sh`（使用 `deploy/cloud/docker-compose.yml`）
- `openmemory/docker-compose.yml` 是上游遗留文件，**禁止使用**

## 架构原则

- **编译侧**（Mac Mini / CI worker）：构建镜像、推送到 ACR
- **运行侧**（云端服务器 47.108.141.20）：只拉取镜像、运行服务，无源码
- **配置即代码**：nginx.conf、docker-compose.yml 版本控制，部署时同步

## 版本管理

- **唯一版本来源**: 项目根目录 `VERSION` 文件
- **版本递增**: Patch +0.0.1（小改动）| Minor +0.1.0（Epic）| Major +1.0.0（Release）
- **每次变更**: bump VERSION → commit → 构建镜像

## 架构差异（关键！）

- **Mac Mini**: Apple Silicon arm64 → `--local` 构建 arm64
- **云服务器**: Intel Xeon x86_64/amd64 → `--cloud` 构建 amd64
- **两端不能共用同一份镜像！**

## 构建优化

- 使用 `docker buildx build --load` 构建 amd64 镜像（利用 buildx 缓存）
- **禁止加 `--pull` 参数**，避免每次构建都从 Docker Hub 拉取基础镜像
- 基础镜像首次拉取后会被 buildx 缓存，后续构建自动复用

## 部署脚本 (`deploy/`)

| 脚本 | 用途 | 在哪运行 |
|------|------|----------|
| `build.sh --local` | 构建 arm64 本地镜像 | Mac Mini |
| `build.sh --cloud` | 构建 amd64 云端镜像 | Mac Mini |
| `push.sh` | 推送 amd64 镜像到 ACR | Mac Mini |
| `deploy-local.sh` | 本地编译 + 部署 | Mac Mini |
| `deploy-cloud.sh` | 云端完整部署 | Mac Mini (SSH) |
| `rollback-cloud.sh` | 云端回滚 | Mac Mini (SSH) |
| `setup-cloud.sh` | 首次初始化云端 workspace | Mac Mini (SSH) |

### 典型工作流

```bash
# 日常开发
./deploy/deploy-local.sh

# 发布到云端
./deploy/deploy-cloud.sh               # 完整: 编译 + 推送 + 部署
./deploy/deploy-cloud.sh --skip-build  # 仅推送 + 部署

# 紧急回滚
./deploy/rollback-cloud.sh
./deploy/rollback-cloud.sh --full      # 含数据库回滚
```

## 云端目录结构 (`~/workspace/`)

```
~/workspace/
  docker-compose.yml   # 从 deploy/cloud/ 同步
  .env                 # 密钥在服务器维护
  nginx/
    nginx.conf         # 从 deploy/cloud/ 同步
    cert/              # SSL 证书 (服务器管理)
  backup/              # 按版本号备份
```

## ACR 镜像仓库

- 公网: `crpi-xsgmoqneyleca8gg.cn-chengdu.personal.cr.aliyuncs.com`
- 内网: `crpi-xsgmoqneyleca8gg-vpc.cn-chengdu.personal.cr.aliyuncs.com`
- Namespace: `arthaszeng`
- 镜像 tag 格式: `:VERSION-amd64` + `:latest`
