# Compose-level variables (used for ${VAR} substitution in docker-compose files)
# Copy to .env in the same directory as docker-compose.cloud.yml

MEM0_USER=arthas
PUBLIC_API_URL=http://localhost:8765
